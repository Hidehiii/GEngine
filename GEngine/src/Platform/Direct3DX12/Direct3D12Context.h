#pragma once
#include "GEngine/Core/Core.h"
#include "GEngine/Graphics/GraphicsContext.h"
#include <windows.h>
#include <wrl.h>
#include <dxgi1_4.h>
#include <d3d12.h>

namespace GEngine
{
	/*class Direct3D12Context : public  GraphicsContext
	{
	public:
		Direct3D12Context(void* windowHandle);
		virtual ~Direct3D12Context() override;

		virtual void				Init(const unsigned int width, const unsigned int height) override;
		virtual void				Uninit() override;
		virtual void				SwapBuffers() override;
		virtual void				SetVSync(bool enable) override;
		virtual void				SetRequiredExtensions(std::vector<const char*> extensions) override;

	};*/
}


